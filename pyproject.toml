[project]
name = "fine-tuning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "axolotl[deepspeed,flast-attn]>=0.12.2",
    "datasets>=4.0.0",
    "unsloth-zoo[base]>=2025.7.4",
    "unsloth[base]>=2025.7.2",
    "wandb>=0.23.1",
]
